program Consultor;

uses
  System.StartUpCopy,
  FMX.Forms,
  u_main in 'u_main.pas' {f_main};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(Tf_main, f_main);
  Application.Run;
end.
